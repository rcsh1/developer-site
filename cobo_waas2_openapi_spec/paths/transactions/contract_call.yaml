post:
  tags:
    - Transactions
  operationId: create_contract_call_transaction
  summary: Create transaction to call smart contract #dev Upon successful creation, a transaction request is returned, which can then be submitted to the blockchain for execution.这句是否可以删除.body 里没有 function to call 这个属性
  description: |
    This operation creates a transaction to call a function within a smart contract on the blockchain.
    The request body must include details such as the source address, destination address, and the function to call.
    Note: Currenly this operation only applies to ethereum-like blockchains.
  security:
    - CoboAuth: []
  requestBody:
    $ref: "../../components/request_bodies/transactions/create_smart_contract_call_transaction.yaml"
  responses:
    '201':
      $ref: "../../components/responses/transactions/create_transaction_request.yaml"
    '400':
      $ref: "../../components/responses/errors/400_bad_request.yaml"
