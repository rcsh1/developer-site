post:
  tags:
    - Transactions
  operationId: create_transfer_transaction
  summary: Create a transfer transaction
  description: |
    This operation transfers funds from one account to another based on the details that you provide, such as the source and destination addresses, token ID, and amount to be transferred. #dev fund 是什么概念？token 吗
    Upon successful creation, a transaction request is returned, which can then be used for tracking the process. #dev 感觉每个 post 类型的操作应该都是返回  transaction request 不需要说吧
  security:
    - CoboAuth: []
  requestBody:
    $ref: "../../components/request_bodies/transactions/create_transfer_transaction.yaml"
  responses:
    '201':
      $ref: "../../components/responses/transactions/create_transaction_request.yaml"
    '400':
      $ref: "../../components/responses/errors/400_bad_request.yaml"
