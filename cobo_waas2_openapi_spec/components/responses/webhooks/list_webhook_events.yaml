description: A list of webhook events has been successfully retrieved.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          items:
            $ref: '../../schemas/webhooks/event.yaml'
          type: array
        pagination:
          $ref: '../../schemas/pagination/metadata.yaml'