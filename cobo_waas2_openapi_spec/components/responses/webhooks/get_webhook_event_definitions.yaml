description: The request was successful.
content:
  application/json:
    schema:
      type: array
      items:
        type: object
        properties:
          event_type:
            type: string
            example: "asset_wallet.outbound.created"
            description: The event type.
          description:
            type: string
            example: "Occurs when a request to transfer from an Asset Wallet is created."
            description: The event type description.