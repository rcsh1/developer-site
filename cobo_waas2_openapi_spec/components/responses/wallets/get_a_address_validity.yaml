description: Successfully get address validity
content:
  application/json:
    schema:
      type: object
      properties:
        validity:
          type: boolean
          description: The validity of the address.
          example: true
