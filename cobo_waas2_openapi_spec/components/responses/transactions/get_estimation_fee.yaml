description: Successful get the fee estimation result
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '../../schemas/transactions/fees/estimation_fee.yaml'
