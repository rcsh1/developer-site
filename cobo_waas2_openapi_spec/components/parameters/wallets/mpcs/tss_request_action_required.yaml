name: tss_request_action
in: query
description: The action of tss request.
required: true
schema:
  type: string
  description: the action of tss request.
  enum: [
    "cancel",
  ]
  example: "cancel"
