name: min_created_timestamp
in: query
required: false
description: Filters transactions where the creation time is greater than or equal to the specified value in Unix timestamp format, measured in milliseconds/seconds. #dev does it mean the earliest creation time
schema:
  type: integer
example: 1635744000
