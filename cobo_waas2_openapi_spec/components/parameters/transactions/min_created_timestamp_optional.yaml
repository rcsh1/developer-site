name: min_created_timestamp
in: query
required: false
description: The time when the transaction was created, in Unix timestamp format, measured in milliseconds/seconds. # what min here means
schema:
  type: integer
example: 1635744000
