name: max_created_timestamp
in: query
required: false
description: Filters transactions where the creation time is smaller than or equal to the specified value in Unix timestamp format, measured in milliseconds/seconds. #dev
schema:
  type: integer
example: 1635744000
