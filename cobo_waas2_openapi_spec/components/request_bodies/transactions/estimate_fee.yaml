description: The request body to estimate the transaction fee of a transfer or a smart-contract call
content:
  application/json:
    schema:
      $ref: '../../schemas/transactions/estimate_fee.yaml'
