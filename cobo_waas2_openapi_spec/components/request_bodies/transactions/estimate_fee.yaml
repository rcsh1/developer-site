description: The request body to estimate the transaction fee of a token transfer or a contract call.
content:
  application/json:
    schema:
      $ref: '../../schemas/transactions/estimate_fee/estimate_fee.yaml'
