type: string
enum: [
  "Address",
  "ExchangeWallet",
]
example: 'Address'
description: |
  The type of the external destination. Possible values include:
  - `Address`: An external address.
  - `ExchangeWallet`: An exchange wallet.