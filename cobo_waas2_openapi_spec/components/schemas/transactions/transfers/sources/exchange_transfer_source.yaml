allOf:
  - $ref: "./base_transfer_source.yaml"
  - type: object
    required:
      - sub_wallet_id
    properties:
      sub_wallet_id:
        type: string
        description: An exchange trading account or a sub-wallet that made the transfer. #
        example: 'Asset'
