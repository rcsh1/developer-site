title: Exchange
allOf:
  - $ref: "./base_transfer_source.yaml"
  - type: object
    required:
      - sub_wallet_id
    properties:
      sub_wallet_id:
        type: string
        description: The exchange trading account or the sub-wallet that the transfer is from. 
        example: 'Asset'
