type: string
enum:
  - Cancel
  - Drop
  - Resend
  - SpeedUp
example: Resend
description: | 
  The transaction replacement type. Possible values include: #dev replace 的概念
    - `Cancel`: The transaction is cancelled.
    - `Drop`: The transaction is dropped.
    - `Resend`: The transaction is resent.
    - `SpeedUp`: The transaction is sped up.

