type: string
enum:
  - Submitted
  - PendingScreening
  - PendingAuthorization
  - Queued
  - PendingSignature
  - Broadcasting
  - Confirming
  - Completed
  - Failed
  - Rejected
example: Submitted
description: |
  The transaction status. Possible values include: #dev
    - `Submitted`: The transaction is submitted.
    - `PendingScreening`: The transaction is pending for screening. by cobo
    - `PendingAuthorization`: The transaction is pending for authorization. by user authorization
    - `Queued`: The transaction is queued. on chain
    - `PendingSignature`: The transaction is pending for signature. private key holder 
    - `Broadcasting`: The transaction is broadcasting.
    - `Confirming`: The transaction is confirming.
    - `Completed`: The transaction is completed.
    - `Failed`: The transaction failed.
    - `Rejected`: The transaction is rejected.

