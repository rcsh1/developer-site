allOf:
  - type: object
    properties:
      mpc_used_key_group:
        $ref: '../../../../mpc_signing_group.yaml'
      broker_user_id:
        type: string
        example: cobo001
  - $ref: "../source.yaml"
