type: object
description: |
  The transaction fee for UTXO-based chains. The estimated fee is calculated by multiplying the fee rate by the transaction size: (fee rate * transaction size). #TBD
required:
  - fee_type
allOf:
  - type: object
    properties:
      fee_type:
        $ref: '../../fees/fee_type.yaml'
      fee_used:
        type: string
        description: The fee amount in fee_coin. # fee coin
        example: "0.1"
  - $ref: '../../fees/base_fee_price_utxo.yaml'
  - $ref: '../../fees/fee_max_fee_amount.yaml'