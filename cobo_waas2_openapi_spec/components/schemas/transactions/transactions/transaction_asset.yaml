allOf:
  - $ref: '../../wallets/asset.yaml'
  - type: object
    description: The data for transaction asset information.
    properties:
      amount:
        type: string
        description: >
          Transaction value (Note that this is an absolute value.
          If you trade 1.5 BTC, then the value is 1.5)
        format: float
        example: '1.5'