type: object
description: The estimated transaction fee when using the EIP 1559 method.
required:
  - fee_type
  - standard
properties:
  fee_type:
    $ref: './fee_type.yaml'
  slow:
    allOf:
      - $ref: './base_fee_price_eip1559.yaml'
      - $ref: './fee_data.yaml'
  standard:
    allOf:
      - $ref: './base_fee_price_eip1559.yaml'
      - $ref: './fee_data.yaml'
  fast:
    allOf:
      - $ref: './base_fee_price_eip1559.yaml'
      - $ref: './fee_data.yaml'
