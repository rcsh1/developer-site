type: object
description: The fee data for transaction.
required:
  - fee_type
allOf:
  - type: object
    properties:
      fee_type:
        $ref: './fee_type.yaml'
  - $ref: './base_fee_price_utxo.yaml'
  - $ref: './fee_max_fee_amount.yaml'