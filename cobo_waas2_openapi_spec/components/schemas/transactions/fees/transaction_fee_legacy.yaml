type: object
description: The fee data for transaction.
required:
  - fee_type
properties:
  fee_type:
    $ref: './fee_type.yaml'
allOf:
  - $ref: './base_fee_price_legacy.yaml'
  - $ref: './fee_data.yaml'