type: object
description: The base transaction fee when using the EIP 1559 method.
required:
  - max_fee
  - max_priority_fee
  - base_fee
properties:
  fee_token_id:
    type: string
    description: The token ID of the transaction fee. Unique in all chains scope. # Unique in all chains scope. 是什么意思？
    example: 'ETH'
  max_fee:
    type: string
    description: The max fee, in gwei. The max fee represents the total gas price you are willing to pay for your transaction.
    example: '1'
  max_priority_fee:
    type: string
    description: The max priority fee, in gwei. The max priority fee represents the highest amount of miner tips you are willing to pay for your transaction.
    example: '0.1'
  base_fee:
    type: string
    description: The base fee of chain.
    example: '0.9'

