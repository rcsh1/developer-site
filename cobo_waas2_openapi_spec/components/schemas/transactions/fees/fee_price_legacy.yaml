type: object
description: The legacy fee data for estimate fees or transfer.
required:
  - fee_type
allOf:
  - type: object
    properties:
      fee_type:
        $ref: './fee_type.yaml'
  - $ref: './base_fee_price_legacy.yaml'

