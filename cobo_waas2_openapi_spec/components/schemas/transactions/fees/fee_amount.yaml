type: object
description: The estimated fee amount in fee_coin.
required:
  - fee_amount
properties:
  fee_amount:
    type: string
    description: The estimated fee amount in fee_coin.
    example: "0.1"