type: object
description: The legacy fee data for estimate fees or transfer.
required:
  - fee_type
allOf:
  - properties:
      fee_type:
        $ref: './fee_type.yaml'
  - $ref: './base_fee_price_utxo.yaml'
