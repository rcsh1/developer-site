type: string
enum: [
  "Transfer",
  "ContractCall",
  "MessageSign"
]
example: 'Transfer'
description: |
  The request type. Possible values include:
    - `Transfer`: A transfer transaction request.
    - `ContractCall`: A transaction request to interact with a small contract.
    - `MessageSign`: A transaction request to sign a message.
