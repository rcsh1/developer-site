type: object
description: The balance information.
required:
  - token_id
  - balance
properties:
  token_id:
    type: string
    description: The token ID.
    pattern: ^[A-Z]+(_[A-Z0-9]+)?$
    example: 'ETH_USDT'
  balance:
    $ref: './balance.yaml'
