type: string
enum: [
    "KeyGen",
    "KeyGenFromKeyGroup",
    "Recovery",
]
example: "KeyGen"
description: |
    The type of tss request.
    KeyGen is key generation
    KeyGenFromKeyGroup is generate RecoveryKeyGroup secret
    Recovery is generate MainKeyGroup secret from RecoveryKeyGroup
