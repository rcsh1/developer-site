allOf:
  - $ref: '../bases/base_wallet_info.yaml'
  - type: object
    required:
      - vault_id
    properties:
      vault_id:
        type: string
        description: The ID of the owning Vault.
