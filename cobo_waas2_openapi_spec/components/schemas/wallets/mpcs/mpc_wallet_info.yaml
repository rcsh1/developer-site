allOf:
  - $ref: '../bases/base_wallet_info.yaml'
  - type: object
    required:
      - vault_id
    properties:
      vault_id:
        type: string
        description: "The owning mpc vault id of the mpc wallet."
