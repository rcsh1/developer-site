allOf:
  - $ref: "../bases/base_wallet_info.yaml"
  - type: object
    properties:
      chain_id:
        type: string
        description: The ID of the chain on which the wallet operates. This property is applicable to Smart Contract Wallets only. 
        example: "ETH"
      label:
        type: string
        description: The wallet label. This property is applicable to Smart Contract Wallets only. 
        example: "My Wallet"
