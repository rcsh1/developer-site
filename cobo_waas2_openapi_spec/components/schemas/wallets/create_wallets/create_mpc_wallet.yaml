allOf:
  - $ref: './base_create_wallet.yaml'
  - type: object
    required:
      - wallet_type
      - wallet_subtype
      - vault_id
    properties:
      wallet_type:
        type: string
        enum:
          - MPC
        description: The wallet type.
      wallet_subtype: # 要加解释
        type: string
        enum:
          - Org-Controlled
          - User-Controlled
      vault_id:
        type: string
        description: The ID of the owning vault.
