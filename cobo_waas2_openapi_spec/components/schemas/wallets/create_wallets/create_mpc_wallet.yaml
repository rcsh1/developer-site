type: object
description: The information of MPC Wallets.
allOf:
  - $ref: './create_custodial_wallet.yaml'
  - type: object
    required:
      - vault_id
    properties:
      vault_id:
        type: string
        description: The ID of the owning vault. This property is applicable to MPC Wallets only. 
