allOf:
  - $ref: './base_create_wallet.yaml'
  - type: object
    required:
      - wallet_type
      - wallet_subtype
    properties:
      wallet_type:
        type: string
        enum:
          - Custodial # 为啥只有一个 type？
        description: The wallet type.
      wallet_subtype:
        type: string
        enum:
          - Asset #需要加解释
          - Web3