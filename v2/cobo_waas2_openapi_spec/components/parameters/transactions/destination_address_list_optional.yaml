name: destination_address
in: query
required: false
description: |
  The address of the transaction destination. If the address includes a memo, the format should be `address|memo`.
schema:
  description: A list of addresses, separated by comma.
  type: string