name: chain_id
in: path
required: true
description: The chain ID. You can retrieve chain IDs by using the [List enabled chains](/v2/api-references/wallets/list-enabled-chains) operation.
schema:
  type: string
example: 'ETH'
