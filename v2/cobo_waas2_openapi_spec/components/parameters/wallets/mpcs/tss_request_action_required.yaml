name: tss_request_action
in: query
description: |
  The action of the TSS request to be taken. Possible values include:
  - `cancel`: This action cancels the TSS request.
required: true
schema:
  $ref: "../../../schemas/wallets/mpcs/tss_request_action.yaml"
