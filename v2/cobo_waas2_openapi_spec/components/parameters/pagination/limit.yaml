name: limit
in: query
description: The maximum number of objects to return. For most operations, the value range is [1, 50].
required: false
schema:
  type: integer
  format: int32
  default: 10
example: 10
