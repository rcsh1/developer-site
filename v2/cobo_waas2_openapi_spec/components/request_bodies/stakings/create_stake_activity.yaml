description: The request body to create a staking activity.
content:
  application/json:
    schema:
      allOf:
        - $ref: '../../schemas/stakings/create_stake_activity.yaml'
        - $ref: '../../schemas/stakings/activity_initiator.yaml'

