description: Successfully listed addresses
content:
  application/json:
    schema:
        type: object
        properties:
          data:
            type: array
            items:
              $ref: '../../schemas/wallets/addresses/address_info.yaml'
          pagination:
            $ref: '../../schemas/pagination/metadata.yaml'
