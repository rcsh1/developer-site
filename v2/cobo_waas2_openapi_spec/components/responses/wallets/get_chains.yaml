description: The request was successful.
content:
  application/json:
    schema:
        type: object
        properties:
          data:
            type: array
            items:
              $ref: '../../schemas/wallets/chain.yaml'
          pagination:
            $ref: '../../schemas/pagination/metadata.yaml'
