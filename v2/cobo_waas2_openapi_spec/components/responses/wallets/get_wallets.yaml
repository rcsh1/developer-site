description: Successfully listed wallets
content:
  application/json:
    schema:
        type: object
        properties:
          data:
            type: array
            items:
              $ref: '../../schemas/wallets/wallet_info.yaml'
          pagination:
            $ref: '../../schemas/pagination/metadata.yaml'
