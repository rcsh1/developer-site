description: A list of staking activities has been successfully retrieved.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          items:
            $ref: '../../schemas/stakings/activity.yaml'
          type: array
        pagination:
          $ref: '../../schemas/pagination/metadata.yaml'