description: A list of staking has been successfully retrieved.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          items:
            $ref: '../../schemas/stakings/staking.yaml'
          type: array
        pagination:
          $ref: '../../schemas/pagination/metadata.yaml'