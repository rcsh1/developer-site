description: The request was successful.
content:
  application/json:
    schema:
      type: object
      properties:
        fee_type:
          $ref: '../../schemas/transactions/fees/fee_type.yaml'
        fee_amount:
          type: string
          description: "The amount of the estimate transaction fee"
          example: "0.02"
        token_id:
          type: string
          description: The token ID of the transaction fee.
          example: 'ETH'