description: A list of staking protocols has been successfully retrieved.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          items:
            $ref: '../../schemas/stakings/pool_summary.yaml'
          type: array
        pagination:
          $ref: '../../schemas/pagination/metadata.yaml'