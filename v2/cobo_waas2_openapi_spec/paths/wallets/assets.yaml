get:
  tags:
    - Wallets
  operationId: get_assets
  summary: List asset metadata
  description: |
    This operation retrieves the metadata of all assets supported by Cobo WaaS 2.0.
    
    It provides details such as asset IDs, asset symbols, and other relevant information. The asset metadata is publicly available without any permission restrictions.
  security:
    - CoboAuth: []
  parameters:
    - $ref: "../../components/parameters/wallets/asset_id_optional.yaml"
    - $ref: "../../components/parameters/pagination/limit.yaml"
    - $ref: "../../components/parameters/pagination/before.yaml"
    - $ref: "../../components/parameters/pagination/after.yaml"
  responses:
    '200':
      $ref: "../../components/responses/wallets/get_assets.yaml"
    '400':
      $ref: "../../components/responses/errors/400_bad_request.yaml"